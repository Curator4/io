{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2022",                          // What version of JS to compile to
    "lib": ["ES2022"],                           // Standard library types to include

    /* Modules */
    "module": "ESNext",                          // Use modern ES modules (import/export)
    "moduleResolution": "node",                  // How to resolve module imports
    "resolveJsonModule": true,                   // Allow importing .json files

    /* Emit */
    "outDir": "./dist",                          // Where compiled .js files go
    "sourceMap": true,                           // Generate .map files for debugging
    "declaration": true,                         // Generate .d.ts type declaration files

    /* Interop Constraints */
    "esModuleInterop": true,                     // Better compatibility with CommonJS modules
    "forceConsistentCasingInFileNames": true,    // Enforce consistent file name casing

    /* Type Checking */
    "strict": true,                              // Enable all strict type checks
    "noImplicitAny": true,                       // Error on variables without explicit types
    "strictNullChecks": true,                    // null and undefined are separate types
    "noUnusedLocals": true,                      // Error on unused local variables
    "noUnusedParameters": true,                  // Error on unused function parameters
    "noImplicitReturns": true,                   // Error if function doesn't return explicitly

    /* Completeness */
    "skipLibCheck": true                         // Skip type checking of .d.ts files (faster)
  },
  "include": ["src/**/*"],                       // Which files to compile
  "exclude": ["node_modules", "dist"]            // Which files to ignore
}
